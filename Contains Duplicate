class Solution{
    public: 
    bool containsDuplicate(vector<int>& nums){
        unordered_set<int> ints;
        for(int num : nums){
            if (!ints.insert(num).second)
            {
                return true;
            }
        }
        return false;
    }
};

